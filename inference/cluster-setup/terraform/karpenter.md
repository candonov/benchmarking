# Set Up Cluster and Nodes (Self-managed Karpenter, Terraform)

> **Placeholder.** Terraform-based walkthrough for the self-managed Karpenter equivalent of the [CLI guide](../cli/cluster-and-compute/karpenter.md) goes here.

This walkthrough will cover provisioning an EKS standard cluster with a self-managed Karpenter controller and a GPU NodePool with Terraform, equivalent to what the CLI version creates with `eksctl`.
